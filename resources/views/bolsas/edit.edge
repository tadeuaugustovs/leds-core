<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Editar Bolsa</title>
  <style>
    label { display: block; margin-top: 10px; }
    input { width: 100%; padding: 5px; }
    button { padding: 8px 16px; background: #4CAF50; color: white; border: none; border-radius: 4px; margin-top: 10px; }
  </style>
</head>
<body>
  <h1>Editar Bolsa</h1>

  <!-- ATENÇÃO: alterei a rota e tirei o spoofing -->
  <form action="/bolsas/{{ bolsa.id }}/editar" method="POST">

    <label>Nome:</label>
    <input type="text" name="nome" value="{{ bolsa.nome }}" required>

    <label>Valor (R$):</label>
    <input type="number" step="0.01" name="valor" value="{{ bolsa.valor }}" required>

    <label>Tipo Bolsa ID:</label>
    <input type="number" name="tipo_bolsa_id" value="{{ bolsa.tipo_bolsa_id }}" required>

    <label>Patrocinador:</label>
    <input type="text" name="patrocinador" value="{{ bolsa.patrocinador || '' }}">

    <label>Quantidade de Membros:</label>
    <input type="number" name="quantidade_membros" value="{{ bolsa.quantidade_membros || 0 }}">

    <button type="submit">Salvar Alterações</button>
  </form>

  <br>
  <a href="/bolsas">Voltar para a lista</a>
</body>
</html>
